<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      ============================================================
      DEPRECATED — legacy `app_*` spacing tokens (Patch 18 / v37).

      Phase 9 (Layout Migration) moved every reference to the
      semantic design tokens in design_system.xml:
        app_screen_edge_h        -> ?attr/livreScreenPadding
        app_card_padding        -> ?attr/livreCardPadding
        app_row_spacing          -> ?attr/livreRowSpacing
        app_row_content_padding_h-> @dimen/row_content_padding_h
        app_card_corner          -> @dimen/card_corner_radius
        app_selection_toolbar_*  -> @dimen/selection_toolbar_*
        (full mapping in docs/PHASE_9_LAYOUT_MIGRATION.md)

      This file intentionally defines no dimensions.
      ============================================================
    -->
</resources>
