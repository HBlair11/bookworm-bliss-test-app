# Patch H — Duplicate bookmark Delete + Undo

Delete from the `Bookmark exists` snackbar now deletes only the matching bookmark and immediately offers the established Undo action. This applies to both whole-page and selected-text bookmarks.
